<div class="content_left">
	<h1><%= @artist.name %></h1>
	<%= image_tag @artist.profile_picture.url %>
	<%= simple_format(@artist.bio) %>
</div>
<div class="content_right">
	<%= render 'layouts/youtube_video.html.erb', 
		youtube_token: @artist.youtube_token %>
</div>